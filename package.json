{
  "name": "xrsps",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22.16.0"
  },
  "scripts": {
    "setup": "yarn install --frozen-lockfile && yarn --cwd server install --frozen-lockfile && yarn --cwd client install --frozen-lockfile",
    "start": "yarn concurrently --kill-others --names server,client \"yarn --cwd server dev\" \"yarn --cwd client start\"",
    "server": "yarn --cwd server dev",
    "client": "yarn --cwd client start"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
